{
  "name": "@apps/api",
  "version": "1.0.0",
  "private": true,
  "exports": {
    "./*": [
      "./src/*.ts",
      "./src/*.tsx",
      "./src/*/index.ts"
    ]
  },
  "scripts": {
    "build": "test -f Dockerfile",
    "deploy": "bun run scripts/deploy.ts",
    "dev": "bun --env-file=.env run src/api.ts",
    "lint": "eslint src --max-warnings 0",
    "setup": "bun run scripts/setup-dev-vars.ts",
    "tc": "tsc --noEmit -p ../../tsconfig.json",
    "test": "bun test"
  },
  "dependencies": {
    "@pkgs/assert": "workspace:*",
    "@pkgs/logger": "workspace:*",
    "@pkgs/object-store": "workspace:*",
    "@pkgs/secret-store": "workspace:*",
    "hono": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "typescript": "catalog:"
  }
}
